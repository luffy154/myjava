# application.yml
spring:
  mail:
    primary:
      host: smtp.qq.com       # SMTP服务器地址smtp.feishu.cn
      port: 587                    # 安全端口（TLS）587
      username: 1318726761@qq.com   # 发件邮箱
      password: lhsxnnouffeggbii      # 邮箱密码/授权码
      protocol: smtp
      properties:
        mail:
          smtp:
            auth: true
            starttls:
              enable: true         # 启用TLS加密
            connectiontimeout: 2000
            timeout: 5000
            writetimeout: 5000
  redis:
    host: 10.10.0.5
    port: 6379
    password: kT9!@fx@vK6sBLLU
    database: 8
    lettuce:
      pool:
        max-active: 100
        max-wait: 2s
        min-idle: 10
        max-idle: 50
        time-between-eviction-runs: 5m
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.jdbc.DataSourceTransactionManagerAutoConfiguration
      - org.springframework.boot.autoconfigure.r2dbc.R2dbcAutoConfiguration
      - org.springframework.boot.autoconfigure.admin.SpringApplicationAdminJmxAutoConfiguration
  application:
    admin:
      jmx:
        enabled: false

custom:
  mail:
    qq-mail:
      host: smtp.qq.com       # SMTP服务器地址smtp.feishu.cn
      port: 587                    # 安全端口（TLS）587
      username: 1548476679@qq.com   # 发件邮箱
      password: lhsxnnouffeggbii      # 邮箱密码/授权码
      protocol: smtp
      properties:
        mail:
          smtp:
            auth: true
            starttls:
              enable: true         # 启用TLS加密
            connectiontimeout: 2000
            timeout: 5000
            writetimeout: 5000
    feishu-mail:
      host: smtp.feishu.cn       # SMTP服务器地址smtp.feishu.cn
      port: 587                    # 安全端口（TLS）587
      username: software-dept@hf-lm.com   # 发件邮箱
      password: dlJOhYBypT21ryiQ      # 邮箱密码/授权码
      protocol: smtp
      properties:
        mail:
          smtp:
            auth: true
            starttls:
              enable: true         # 启用TLS加密
            connectiontimeout: 2000
            timeout: 5000
            writetimeout: 5000

dubbo:
  application:
    name: user-service
  registry:
    address: nacos://10.10.0.13:8848?namespace=66b32662-ccbe-4376-91f5-510a0bcdc152&group=DEFAULT_GROUP
    username: nacos
    password: hfnacos
  protocol:
    name: dubbo
    port: 20880
    serialization: hessian2
  reference:
    com.report.test.dubbo.service.PretectService:
      loadbalance: minweight